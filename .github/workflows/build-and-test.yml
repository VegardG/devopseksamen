on:
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build code
        run: mvn spring-boot:run
      - name: Run tests
        run: mvn test